{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for TrackHelm",
  "platforms": ["macOS", "windows", "linux"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
